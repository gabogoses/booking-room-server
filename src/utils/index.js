const decodeToken = require('./decodeToken');
const sendMail = require('./sendMail');
const signToken = require('./signToken');
const isRoomBooked = require('./isRoomBooked');

module.exports = {
    decodeToken,
    sendMail,
    signToken,
    isRoomBooked,
};
